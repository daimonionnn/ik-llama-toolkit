# =============================================================================
# Qwen3.8-Flash-Next Q8_0 -- alias for the 128k profile
# =============================================================================
# The unsuffixed name is kept so `./serve.sh qwen38-flash-next-q8` works. Both
# real profiles are:
#
#   qwen38-flash-next-q8-128k    -ub 2048   <-- this one
#   qwen38-flash-next-q8-256k    -ncmoe 20, and see its host-RAM warning
# -----------------------------------------------------------------------------

source "$(dirname "${BASH_SOURCE[0]}")/qwen38-flash-next-q8-128k.env"
