# depthbench, `-ub 8192`

2 repeat(s) per depth, `max_tokens` 160, temperature 0, unique salt per
request so nothing is served from the prompt cache.

## Configuration measured

(read back from the server log, not from what was asked for)

| setting | value |
|---|---|
| `n_ubatch` | 8192 |
| `n_batch` | 8192 |
| `n_ctx` | 131072 |
| `-rtr` | off |
| `prompt cache` | on |
| `CUDA0 weights` | 89366.93 MiB |
| `host weights` | 59762.00 MiB |
| `compute buffer` | 4991.80 MiB |

## Results

| depth (tokens) | prefill tok/s | generation t/s | prefill spread |
|---:|---:|---:|---:|

## INCOMPLETE

The run stopped at depth 20000, repeat 1: `RemoteDisconnected('Remote end closed connection without response')`

The rows above are still valid; the depths below were never run.

**Cause not established here.** A lost connection means the server went
away, which covers an abort, an OOM, and someone stopping it by hand
equally well. Check the server log before concluding anything -- an
earlier version of this file asserted a cause and was wrong.
