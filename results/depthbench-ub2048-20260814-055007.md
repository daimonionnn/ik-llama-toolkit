# depthbench, `-ub 2048`

4 repeat(s) per depth, `max_tokens` 160, temperature 0, unique salt per
request so nothing is served from the prompt cache.

## Configuration measured

(read back from the server log, not from what was asked for)

| setting | value |
|---|---|
| `n_ubatch` | 2048 |
| `n_batch` | 4096 |
| `n_ctx` | 131072 |
| `-rtr` | on |
| `KV` | host RAM (-nkvo) |
| `prompt cache` | on |
| `CUDA0 weights` | 92630.93 MiB |
| `host weights` | 56498.00 MiB |
| `compute buffer` | 1760.01 MiB |

## Results

| depth (tokens) | prefill tok/s | generation t/s | prefill spread |
|---:|---:|---:|---:|
| 32701 | **485.3** | **20.21** | 6.6 % |

## INCOMPLETE

The server died at depth 128000, repeat 3: `RemoteDisconnected('Remote end closed connection without response')`

The rows above are still valid; the depths below it were never run.

> **Correction, 2026-08-17.** This file originally said the stop "is the abort
> under investigation (TODO item 9), not a benchmark failure". That was wrong:
> the server was stopped by hand, to free the GPU. The tool cannot tell a crash
> from a deliberate shutdown -- a dropped connection looks the same -- and no
> longer claims to. The measurement itself (485.3 pp at 32k, `-ub 2048`) is
> unaffected and is what RESULTS §18.1 quotes.
